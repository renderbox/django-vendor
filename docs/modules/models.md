# Models

This package provides the core data models for Django Vendor, including products, offers, invoices, payments, receipts, subscriptions, and related helpers.

For detailed field definitions, see the docstrings and type hints in `vendor.models`.
